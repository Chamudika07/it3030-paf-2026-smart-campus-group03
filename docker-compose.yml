version: "3.9"

services:
  postgres:
    image: postgres:16-alpine
    container_name: smart-campus-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: smart_campus_ops_hub
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:

